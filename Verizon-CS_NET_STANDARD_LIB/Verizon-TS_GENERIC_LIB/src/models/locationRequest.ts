/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { AccuracyMode, accuracyModeSchema } from './accuracyMode';
import { CacheMode, cacheModeSchema } from './cacheMode';
import { DeviceInfo, deviceInfoSchema } from './deviceInfo';

/** The body contains the the account name and list of devices that you want to locate, plus other options. */
export interface LocationRequest {
  /** Account identifier in "##########-#####". */
  accountName: string;
  /** Device list. */
  deviceList: DeviceInfo[];
  /** Accurary, currently only 0-coarse supported. */
  accuracyMode?: AccuracyMode;
  /** Location cache mode. */
  cacheMode?: CacheMode;
  additionalProperties?: Record<string, unknown>;
}

export const locationRequestSchema: Schema<LocationRequest> = typedExpandoObject(
  {
    accountName: ['accountName', string()],
    deviceList: ['deviceList', array(lazy(() => deviceInfoSchema))],
    accuracyMode: ['accuracyMode', optional(accuracyModeSchema)],
    cacheMode: ['cacheMode', optional(cacheModeSchema)],
  },
  'additionalProperties',
  optional(unknown())
);
