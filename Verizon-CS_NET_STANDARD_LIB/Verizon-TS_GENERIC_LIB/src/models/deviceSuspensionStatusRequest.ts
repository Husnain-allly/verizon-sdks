/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  DeviceFilterWithoutAccount,
  deviceFilterWithoutAccountSchema,
} from './deviceFilterWithoutAccount';
import { DeviceId, deviceIdSchema } from './deviceId';

/** Request to return service suspension information about one or more devices. */
export interface DeviceSuspensionStatusRequest {
  /** The devices that you want to include in the request, specified by device identifier. You only need to provide one identifier per device. */
  deviceIds?: DeviceId[];
  /** Filter for devices without account. */
  filter?: DeviceFilterWithoutAccount;
  /** The name of a billing account. */
  accountName?: string;
  additionalProperties?: Record<string, unknown>;
}

export const deviceSuspensionStatusRequestSchema: Schema<DeviceSuspensionStatusRequest> = typedExpandoObject(
  {
    deviceIds: ['deviceIds', optional(array(lazy(() => deviceIdSchema)))],
    filter: ['filter', optional(lazy(() => deviceFilterWithoutAccountSchema))],
    accountName: ['accountName', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
