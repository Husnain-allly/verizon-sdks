/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  AccountDeviceList,
  accountDeviceListSchema,
} from './accountDeviceList';

/** Returns the name, description, and list of devices in a device group. */
export interface DeviceGroupDevicesData {
  /** The description of the device group. */
  description?: string;
  /** The devices in the device group. */
  devices?: AccountDeviceList[];
  /** False for a status 200 response.True for a status 202 response, indicating that there is more data to be retrieved. */
  hasMoreData?: boolean;
  /** The name of the device group. */
  name?: string;
  additionalProperties?: Record<string, unknown>;
}

export const deviceGroupDevicesDataSchema: Schema<DeviceGroupDevicesData> = typedExpandoObject(
  {
    description: ['description', optional(string())],
    devices: ['devices', optional(array(lazy(() => accountDeviceListSchema)))],
    hasMoreData: ['hasMoreData', optional(boolean())],
    name: ['name', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
