/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { V2AccountDevice, v2AccountDeviceSchema } from './v2AccountDevice';

/** List of device information for an account. */
export interface V2AccountDeviceList {
  /** Account name. */
  accountName: string;
  /** Has more device flag? */
  hasMoreData: boolean;
  /** Last seen device identifier. */
  lastSeenDeviceId?: string;
  /** Maximum page size. */
  maxPageSize: number;
  /** Account device list. */
  deviceList: V2AccountDevice[];
  additionalProperties?: Record<string, unknown>;
}

export const v2AccountDeviceListSchema: Schema<V2AccountDeviceList> = typedExpandoObject(
  {
    accountName: ['accountName', string()],
    hasMoreData: ['hasMoreData', boolean()],
    lastSeenDeviceId: ['lastSeenDeviceId', optional(string())],
    maxPageSize: ['maxPageSize', number()],
    deviceList: ['deviceList', array(lazy(() => v2AccountDeviceSchema))],
  },
  'additionalProperties',
  optional(unknown())
);
