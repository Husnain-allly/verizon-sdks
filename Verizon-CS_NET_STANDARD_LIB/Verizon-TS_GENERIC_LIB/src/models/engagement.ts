/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { AccountService, accountServiceSchema } from './accountService';

/** The engagements associated with the account. */
export interface Engagement {
  /** The engagement ID. */
  engagementId?: string;
  /** The charging group name. */
  chargingGroup?: string;
  /** The services associated with the account. */
  services?: AccountService[];
  additionalProperties?: Record<string, unknown>;
}

export const engagementSchema: Schema<Engagement> = typedExpandoObject(
  {
    engagementId: ['engagementId', optional(string())],
    chargingGroup: ['chargingGroup', optional(string())],
    services: ['services', optional(array(lazy(() => accountServiceSchema)))],
  },
  'additionalProperties',
  optional(unknown())
);
