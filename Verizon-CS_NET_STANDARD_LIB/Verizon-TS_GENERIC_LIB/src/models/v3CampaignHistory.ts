/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  V3CampaignMetaInfo,
  v3CampaignMetaInfoSchema,
} from './v3CampaignMetaInfo';

/** Campaign history. */
export interface V3CampaignHistory {
  /** Has more report flag? */
  hasMoreData: boolean;
  /** Campaign identifier. */
  lastSeenCampaignId?: string;
  /** Firmware upgrade list. */
  campaignList: V3CampaignMetaInfo[] | null;
  additionalProperties?: Record<string, unknown>;
}

export const v3CampaignHistorySchema: Schema<V3CampaignHistory> = typedExpandoObject(
  {
    hasMoreData: ['hasMoreData', boolean()],
    lastSeenCampaignId: ['lastSeenCampaignId', optional(string())],
    campaignList: [
      'campaignList',
      nullable(array(lazy(() => v3CampaignMetaInfoSchema))),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
