/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { CustomFields, customFieldsSchema } from './customFields';

/** Network connection events for a device during a specified time period. */
export interface ConnectionEvent {
  /** The attributes that describe the connection event. */
  connectionEventAttributes?: CustomFields[];
  /** Currently not used. */
  extendedAttributes?: CustomFields[];
  /** The date and time when the connection event occured. */
  occurredAt?: string;
  additionalProperties?: Record<string, unknown>;
}

export const connectionEventSchema: Schema<ConnectionEvent> = typedExpandoObject(
  {
    connectionEventAttributes: [
      'connectionEventAttributes',
      optional(array(lazy(() => customFieldsSchema))),
    ],
    extendedAttributes: [
      'extendedAttributes',
      optional(array(lazy(() => customFieldsSchema))),
    ],
    occurredAt: ['occurredAt', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
