/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { DeviceId, deviceIdSchema } from './deviceId';

/** SMS messages sent by all M2M devices associated with a billing account. */
export interface SmsMessage {
  /** One or more IDs of the device that sent the message. */
  deviceIds?: DeviceId[];
  /** The contents of the SMS message. */
  message?: string;
  /** The date and time that the message was received by the Verizon ThingSpace Platform. */
  timestamp?: string;
  additionalProperties?: Record<string, unknown>;
}

export const smsMessageSchema: Schema<SmsMessage> = typedExpandoObject(
  {
    deviceIds: ['deviceIds', optional(array(lazy(() => deviceIdSchema)))],
    message: ['message', optional(string())],
    timestamp: ['timestamp', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
