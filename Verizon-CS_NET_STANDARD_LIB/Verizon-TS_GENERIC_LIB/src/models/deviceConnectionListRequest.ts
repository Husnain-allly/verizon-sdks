/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { DeviceId, deviceIdSchema } from './deviceId';

/** Request to list of network connection events for a device during a specified time period. */
export interface DeviceConnectionListRequest {
  /** An identifier for a single device. */
  deviceId: DeviceId;
  /** The earliest date and time for which you want connection events. */
  earliest: string;
  /** The last date and time for which you want connection events. */
  latest: string;
  additionalProperties?: Record<string, unknown>;
}

export const deviceConnectionListRequestSchema: Schema<DeviceConnectionListRequest> = typedExpandoObject(
  {
    deviceId: ['deviceId', lazy(() => deviceIdSchema)],
    earliest: ['earliest', string()],
    latest: ['latest', string()],
  },
  'additionalProperties',
  optional(unknown())
);
