/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { V3LicenseDevice, v3LicenseDeviceSchema } from './v3LicenseDevice';

/** Information for FOTA licenses assigned to devices. */
export interface V3LicenseSummary {
  /** Account identifier. */
  accountName: string;
  /** Total FOTA license count. */
  totalLicenses?: number;
  /** Assigned FOTA license count. */
  assignedLicenses: number;
  /** True if there are more devices to retrieve. */
  hasMoreData: boolean;
  /** Last seen device identifier. */
  lastSeenDeviceId?: string;
  /** Maximum page size. */
  maxPageSize: number;
  /** Device IMEI list. */
  deviceList?: V3LicenseDevice[];
  additionalProperties?: Record<string, unknown>;
}

export const v3LicenseSummarySchema: Schema<V3LicenseSummary> = typedExpandoObject(
  {
    accountName: ['accountName', string()],
    totalLicenses: ['totalLicenses', optional(number())],
    assignedLicenses: ['assignedLicenses', number()],
    hasMoreData: ['hasMoreData', boolean()],
    lastSeenDeviceId: ['lastSeenDeviceId', optional(string())],
    maxPageSize: ['maxPageSize', number()],
    deviceList: [
      'deviceList',
      optional(array(lazy(() => v3LicenseDeviceSchema))),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
