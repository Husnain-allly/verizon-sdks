/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { ReportStatus, reportStatusSchema } from './reportStatus';

export interface AsynchronousLocationRequestResult {
  /** The transaction ID of the report. */
  txid?: string;
  /** Status of the report. */
  status?: ReportStatus;
  /** Estimated number of minutes required to complete the report. */
  estimatedDuration?: string;
  additionalProperties?: Record<string, unknown>;
}

export const asynchronousLocationRequestResultSchema: Schema<AsynchronousLocationRequestResult> = typedExpandoObject(
  {
    txid: ['txid', optional(string())],
    status: ['status', optional(reportStatusSchema)],
    estimatedDuration: ['estimatedDuration', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
