/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  CampaignMetaInfoProtocol,
  campaignMetaInfoProtocolSchema,
} from './campaignMetaInfoProtocol';

/** Available firmware. */
export interface FirmwarePackage {
  /** Firmware name. */
  firmwareName: string;
  /** Firmware from version. */
  firmwareFrom: string;
  /** Firmware to version. */
  firmwareTo: string;
  /** Firmware launch date. */
  launchDate: string;
  /** Firmware release note. */
  releaseNote: string;
  /** Firmware applicable device model. */
  model: string;
  /** Firmware applicable device make. */
  make: string;
  /** Firmware protocol. Valid values include: LWM2M, OMD-DM. */
  protocol: CampaignMetaInfoProtocol;
  additionalProperties?: Record<string, unknown>;
}

export const firmwarePackageSchema: Schema<FirmwarePackage> = typedExpandoObject(
  {
    firmwareName: ['firmwareName', string()],
    firmwareFrom: ['firmwareFrom', string()],
    firmwareTo: ['firmwareTo', string()],
    launchDate: ['launchDate', string()],
    releaseNote: ['releaseNote', string()],
    model: ['model', string()],
    make: ['make', string()],
    protocol: ['protocol', campaignMetaInfoProtocolSchema],
  },
  'additionalProperties',
  optional(unknown())
);
