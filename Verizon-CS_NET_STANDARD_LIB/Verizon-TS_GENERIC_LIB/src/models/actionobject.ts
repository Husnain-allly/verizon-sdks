/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  ChangePlanDetails,
  changePlanDetailsSchema,
} from './changePlanDetails';
import {
  Suspenddetailsobject,
  suspenddetailsobjectSchema,
} from './suspenddetailsobject';

export interface Actionobject {
  suspend?: boolean;
  suspendDetails?: Suspenddetailsobject;
  /** a flag to set if the trigger changes service plans, true, or not, false */
  changePlan?: boolean;
  /** The service plan code to switch to */
  changePlanDetails?: ChangePlanDetails;
  additionalProperties?: Record<string, unknown>;
}

export const actionobjectSchema: Schema<Actionobject> = typedExpandoObject(
  {
    suspend: ['suspend', optional(boolean())],
    suspendDetails: [
      'suspendDetails',
      optional(lazy(() => suspenddetailsobjectSchema)),
    ],
    changePlan: ['changePlan', optional(boolean())],
    changePlanDetails: [
      'changePlanDetails',
      optional(lazy(() => changePlanDetailsSchema)),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
