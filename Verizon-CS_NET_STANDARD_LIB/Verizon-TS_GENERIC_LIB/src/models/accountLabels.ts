/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema';
import { DeviceLabels, deviceLabelsSchema } from './deviceLabels';
import { DeviceList, deviceListSchema } from './deviceList';

/** Maximum of 2,000 objects are allowed in the array. */
export interface AccountLabels {
  devices: DeviceList[];
  label?: DeviceLabels[];
  additionalProperties?: Record<string, unknown>;
}

export const accountLabelsSchema: Schema<AccountLabels> = typedExpandoObject(
  {
    devices: ['devices', array(lazy(() => deviceListSchema))],
    label: ['label', optional(array(lazy(() => deviceLabelsSchema)))],
  },
  'additionalProperties',
  optional(unknown())
);
