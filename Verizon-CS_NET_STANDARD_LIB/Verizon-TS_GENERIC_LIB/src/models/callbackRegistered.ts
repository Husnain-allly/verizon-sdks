/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';

/** Registered callback listener. */
export interface CallbackRegistered {
  /** The billing account number for which callback messages will be sent. */
  accountName: string;
  /** The name of the callback service, which identifies the type and format of messages that will be sent to the registered URL. */
  name: string;
  additionalProperties?: Record<string, unknown>;
}

export const callbackRegisteredSchema: Schema<CallbackRegistered> = typedExpandoObject(
  { accountName: ['accountName', string()], name: ['name', string()] },
  'additionalProperties',
  optional(unknown())
);
