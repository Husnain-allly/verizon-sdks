/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  AccountDeviceList,
  accountDeviceListSchema,
} from './accountDeviceList';

/** Request for device status to check availability of activation. */
export interface DeviceActivationRequest {
  /** The name of a billing account. */
  accountName: string;
  /** Up to 10,000 devices that you want to move to a different account, specified by device identifier. */
  devices: AccountDeviceList[];
  additionalProperties?: Record<string, unknown>;
}

export const deviceActivationRequestSchema: Schema<DeviceActivationRequest> = typedExpandoObject(
  {
    accountName: ['accountName', string()],
    devices: ['devices', array(lazy(() => accountDeviceListSchema))],
  },
  'additionalProperties',
  optional(unknown())
);
