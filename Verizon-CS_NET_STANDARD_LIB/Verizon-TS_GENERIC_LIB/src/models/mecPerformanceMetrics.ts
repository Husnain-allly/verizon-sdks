/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import {
  MecPerformanceQueryResult,
  mecPerformanceQueryResultSchema,
} from './mecPerformanceQueryResult';

/** Response to query the most recent data for Key Performance Indicators (KPIs) like network availability, MEC hostnames and more. */
export interface MecPerformanceMetrics {
  /** Success or Failed. */
  queryStatus?: string;
  /** Timestamp of the query's start, format:mm/dd/yyyy,hr:min:sec. */
  start?: string;
  /** Timestamp of the query's end , format:mm/dd/yyyy, hr:min:sec. */
  end?: string;
  /** MEC performance query result. */
  queryResult?: MecPerformanceQueryResult[];
  additionalProperties?: Record<string, unknown>;
}

export const mecPerformanceMetricsSchema: Schema<MecPerformanceMetrics> = typedExpandoObject(
  {
    queryStatus: ['QueryStatus', optional(string())],
    start: ['Start', optional(string())],
    end: ['End', optional(string())],
    queryResult: [
      'QueryResult',
      optional(array(lazy(() => mecPerformanceQueryResultSchema))),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
