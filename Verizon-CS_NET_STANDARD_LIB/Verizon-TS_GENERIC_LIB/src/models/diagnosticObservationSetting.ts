/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { AttributeSetting, attributeSettingSchema } from './attributeSetting';
import { Device, deviceSchema } from './device';

/** Diagnostic observation settings and attributes for a device. */
export interface DiagnosticObservationSetting {
  /** The name of the billing account for which callback messages will be sent. Format: "##########-#####". */
  accountName?: string;
  /** Identifies a particular IoT device. */
  device?: Device;
  /** Streaming RF parameters for which you want to retrieve diagnostic settings. */
  attributes?: AttributeSetting[];
  additionalProperties?: Record<string, unknown>;
}

export const diagnosticObservationSettingSchema: Schema<DiagnosticObservationSetting> = typedExpandoObject(
  {
    accountName: ['accountName', optional(string())],
    device: ['device', optional(lazy(() => deviceSchema))],
    attributes: [
      'attributes',
      optional(array(lazy(() => attributeSettingSchema))),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
