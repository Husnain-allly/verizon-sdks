/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';

/** Filter out the dates. */
export interface DateFilter {
  /** Only include devices that were added after this date and time. */
  earliest: string;
  /** Only include devices that were added before this date and time. */
  latest: string;
  additionalProperties?: Record<string, unknown>;
}

export const dateFilterSchema: Schema<DateFilter> = typedExpandoObject(
  { earliest: ['earliest', string()], latest: ['latest', string()] },
  'additionalProperties',
  optional(unknown())
);
