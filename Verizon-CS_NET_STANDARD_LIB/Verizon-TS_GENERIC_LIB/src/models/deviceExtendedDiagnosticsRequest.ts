/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';
import { DeviceId, deviceIdSchema } from './deviceId';

/** Request for obtaining device extended diagnostics. */
export interface DeviceExtendedDiagnosticsRequest {
  /** The Verizon billing account that the device belongs to. An account name is usually numeric, and must include any leading zeros. */
  accountName: string;
  /** The device for which you want diagnostic information, specified by the device's MDN. */
  deviceList: DeviceId[];
  additionalProperties?: Record<string, unknown>;
}

export const deviceExtendedDiagnosticsRequestSchema: Schema<DeviceExtendedDiagnosticsRequest> = typedExpandoObject(
  {
    accountName: ['accountName', string()],
    deviceList: ['deviceList', array(lazy(() => deviceIdSchema))],
  },
  'additionalProperties',
  optional(unknown())
);
