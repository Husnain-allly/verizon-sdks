/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  number,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema';
import { PolygonType, polygonTypeSchema } from './polygonType';

/**
 * A Polygon is a type of geometry that represents a collection of points that form a closed ring.
 * NOTE: This API only supports a single polygon in the Polygon geometry, so holes cannot be defines at this point. Support for hole will be added in future releases.
 */
export interface Polygon {
  type: PolygonType;
  coordinates: number[];
  additionalProperties?: Record<string, unknown>;
}

export const polygonSchema: Schema<Polygon> = typedExpandoObject(
  {
    type: ['type', polygonTypeSchema],
    coordinates: ['coordinates', array(number())],
  },
  'additionalProperties',
  optional(unknown())
);
