/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema';

/** Software package information. */
export interface SoftwarePackage {
  /** Software name. */
  softwareName: string;
  /** Software launch date. */
  launchDate: string;
  /** Software release note reserved for future use. */
  releaseNote?: string;
  /** Software applicable device model. */
  model: string;
  /** Software applicable device make. */
  make: string;
  /** LWM2M, OMD-DM or HTTP. */
  distributionType: string;
  /** The platform (Android, iOS, etc.) that the software can be applied to. */
  devicePlatformId: string;
  additionalProperties?: Record<string, unknown>;
}

export const softwarePackageSchema: Schema<SoftwarePackage> = typedExpandoObject(
  {
    softwareName: ['softwareName', string()],
    launchDate: ['launchDate', string()],
    releaseNote: ['releaseNote', optional(string())],
    model: ['model', string()],
    make: ['make', string()],
    distributionType: ['distributionType', string()],
    devicePlatformId: ['devicePlatformId', string()],
  },
  'additionalProperties',
  optional(unknown())
);
