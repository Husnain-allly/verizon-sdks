/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema';
import { AccountLead, accountLeadSchema } from './accountLead';

/** Returns information for all leads associated with an account. */
export interface AccountLeadsResult {
  /** False if no more leads.True if there is more data to be retrieved. */
  hasMoreData?: boolean;
  /** The leads associated with an account. */
  leads?: AccountLead[];
  additionalProperties?: Record<string, unknown>;
}

export const accountLeadsResultSchema: Schema<AccountLeadsResult> = typedExpandoObject(
  {
    hasMoreData: ['hasMoreData', optional(boolean())],
    leads: ['leads', optional(array(lazy(() => accountLeadSchema)))],
  },
  'additionalProperties',
  optional(unknown())
);
